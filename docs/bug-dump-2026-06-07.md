# Bug Dump — 2026-06-07

Captured from a single dump session. Status: **logging complete + screenshots mapped, triage pending.**
Screenshots live in `bug-screenshots/` named by bug ID. See the mapping table at the bottom.
Legend: `[ ]` open · `[~]` needs product decision · `[x]` shipped.
Severity: 🔴 broken/wrong · 🟡 polish/UX · 🟢 nice-to-have/feature.

---

## Theme A — Nikud not respecting the toggle

> Triage insight: there are **two** nikud systems. (1) `stripNikud`/`showNikud` governs Hebrew **word/sentence DATA**. (2) `nk()` governs hardcoded Hebrew **chrome** literals. Bugs below split across both — tag which when fixing.
>
> **Resolution (2026-06-07).** Two root causes, both fixed:
> 1. **Wiring** — non-vocab Hebrew (grammar/article/story sentences, glosses, translations) was rendered raw or via a *strip-only* path (`showNikud ? s : stripNikud(s)`), which never adds points when the toggle is ON because that Hebrew isn't pre-enriched at boot like vocab `word.hebrew`. Switched those to `applyNikud()`/`nk()` (the nikud-map lookup that adds points ON and strips OFF).
> 2. **Map coverage** — `scripts/build-nikud-map.py` only scanned `.js`/`.html`, so React `.tsx` chrome (`נלמד`, `הביתה`, `וכשמוכנים`…) was never collected, and newer `.js` data banks were stale. Extended the glob to `.ts`/`.tsx` and re-ran → `data/nikud-map.json` 2551→3360 entries; all five data banks now 100% covered.

- [x] 🔴 **A1** Listening — translated word ignored nikud toggle. *(DATA)* — `MediaPromptCard.translation` now wrapped in `nk()` (was the one un-wrapped prop).
- [x] 🟡 **A2** Word Journey finish — `נלמד`/`הביתה` no nikud. *(MAP)* — chrome was already `nk()`'d; words were just absent from the map (`.tsx` not scanned). Now present.
- [x] 🟡 **A3** Memory finish — `וכשמוכנים, המשיכו` no nikud. *(MAP)* — same `.tsx`-scan gap; words now in map.
- [x] 🔴 **A4** Articles — sentence + rule glosses ignored toggle. *(DATA + MAP)* — `BlankFillGamePage.renderHebrew` → `applyNikud`; articlesData now 100% covered.
- [x] 🔴 **A5** Progressive — Hebrew sentence + notes no nikud. *(DATA + MAP)* — same BlankFill fix; progressiveData 100% covered.
- [x] 🔴 **A6** Progressive — words inside answers lacked nikud. *(MAP)* — answer glosses go through AnswerGrid `nk()`; missing words now in map.
- [x] 🔴 **A7** Fill-blanks — Hebrew sentence no nikud. *(DATA)* — `🇮🇱 {current.translation}` was raw; now `nk()`. sentences.js was already 100% covered.
- [x] 🔴 **A8** Grammar — option glosses + sentence no nikud. *(DATA + MAP)* — `GrammarGamePage.renderHebrew` → `applyNikud`; grammarQuestions 100% covered.
- [x] 🔴 **A9** Story Time — translation/title/question no nikud. *(DATA + MAP)* — tooltip `h.translation`, `story.title`, `question.question` now `nk()`; stories 100% covered.
- [x] 🟡 **A10** `השמעות שנותרו` ("attempts remaining"). *(CHROME)* — already routed through `nk()` (audioHint); regen confirms all hint words (`השמעות`/`נותרו`/`נגמרו`/`ההשמעות`) are mapped.
- [~] 🟡 **A11** Word Journey recognize stage — case/nikud toggle "no effect" on `CHAMPAGNE`/`שַׁמְפַּנְיָה`. *(CONFIRM INTENT)* — **Finding:** the RecallStage word card already honors both toggles (case on the English card, nikud on the Hebrew card via pre-enriched `word.hebrew` + `stripNikud`). The **case** toggle correctly has no effect on the *Hebrew* card (case is English-only); the nikud toggle does work. Likely a misread of correct behavior — left open pending confirmation. (Also a `CHAMPAGNE` content concern → C7.)

## Theme B — Auto-play / audio behavior

- [x] 🟡 **B1** Picture Match — not auto-playing the word on re-entry. **FIXED 2026-06-08:** matched Listening's resume behavior — added a `resumedRef` so a restored question still voices the word on re-entry but skips re-spending the audio budget / reveal gate. (Fresh entry already auto-played.)
- [x] 🟡 **B2** Grammar-beginner — didn't auto-play on entry. **FIXED 2026-06-08:** its autoplay fired at a bare 300ms with no voice-readiness poll, so the FIRST question's audio silently no-opped before voices loaded. Added the same `speechManager` readiness gate the other games use.
- [x] 🟡 **B3** Pronunciation — didn't auto-play word on re-entry. **FIXED 2026-06-08:** resume set `autoPlayedRef=true` (to dodge Chrome's autoplay policy), but navigating into the game IS the user gesture, so it now voices on entry AND re-entry like Listening.
- [x] 🔴 **B4** Pronunciation — auto-advanced on correct without the player pressing "next". **FIXED 2026-06-08:** removed the auto-advance timer (and now-dead `advanceTimer`/`ADVANCE_ON_CORRECT_MS`); the footer "next" already renders on every answered state, so correct answers wait for it (child can replay / hear themselves first). Test updated.
- [x] 🟡 **B5** Grammar — visual-only hint in (parens)/[brackets] (e.g. `I have two ___ (apple)`) was read aloud by TTS. **FIXED 2026-06-08:** added `stripSpokenHints()` applied to both TTS paths (autoplay/manual-play prompt + post-answer full sentence). Hint stays *visible*, just isn't spoken.

## Theme C — Wrong content / data

- [x] 🔴 **C1** Listening — `dozen` translated as `עשרה` (=ten). **FIXED 2026-06-07:** translation → `תריסר` in `data/categories/numbers.js` (+ `תְּרֵיסַר` added to `nikud-map.json`). Image `1️⃣2️⃣` was already correct (12), left as-is.
- [~] 🔴 **C2** Word Journey — plays audio for "cannon" but shows `🎯` (target). Translation `תותח` is correct; only the emoji is wrong. **PARKED 2026-06-07** — no real "cannon" emoji exists in Unicode, so the user will supply a real picture instead. Drop-in when ready: set `imageUrl: "<url>"` on the `Cannon` entry in `data/categories/gaming.js` (or add a `gaming:cannon` override). `WordJourneyPicture` already prefers `word.imageUrl || wordImageOverrides['gaming:cannon']` over the emoji, so no code change needed.
- [~] 🟡 **C3** `Creeper` (minecraft category) — image `💥` shows the *explosion* a creeper causes, not the mob; no emoji depicts a creeper. Translation `קריפר` (transliteration) is correct. **PARKED 2026-06-07** for a real picture (same as C2). Drop-in matches the existing local-icon convention: place `creeper.png` in `img/icons/minecraft/` and add `imageUrl: "img/icons/minecraft/creeper.png"` to the `Creeper` entry in `data/categories/minecraft.js` (cf. `Mine`/`Emerald`). Don't wire the `imageUrl` before the asset exists — it'd render a broken image. Other no-good-emoji minecraft mobs likely want the same later: Enderman 👾, Ghast 👻, Blaze/Nether 🔥, Redstone 🔴, Villager 🧑.
- [x] 🟡 **C4** ABC — the listen-&-pick-the-letter (`letter-sound`) subtype rendered the prompt as a literal `?`. **FIXED 2026-06-07 (N1):** `LetterGlyph` in `ABCGamePage.tsx` now renders a `🔊` emoji (role=img, aria-label="audio") for `letter-sound` instead of `?`, so it reads as audio-only, not broken. The letter stays hidden as intended. Regression test in `react-routes.spec.js` (ABC suite).
- [x] 🟡 **C5** `Goat` → `עז` is correct, but stored with **wrong nikud**. *(source-data nikud, not a code bug)* — **FIXED 2026-06-07:** Dicta had returned `עֹז` (holam = "strength/might"); goat is `עֵז` (tsere). Manual override applied in `data/nikud-map.json`. This survives re-runs because `build-nikud-map.py` skips keys already present in the map (`t not in existing_map`).
- [x] 🟡 **C6** Story Time (koala story) — new words (`Park`, `Fast`) aren't "known" to the engine, so they're not highlighted/translatable. **FIXED 2026-06-08 (via E6):** `buildStoryFromTemplate` now auto-detects every vocab-bank word in the story text that the child hasn't learned and registers it as a highlight, so `park`/`fast`/`runs`/etc. are now tappable + translatable.
- [x] 🟡 **C7** Content audit — flag/replace vocab unsuitable for ages 5–8 (surfaced: `CHAMPAGNE`). **FIXED 2026-06-08:** swept all ~900 words across 25 categories; only the three alcohol entries in `food` were age-inappropriate. Swapped (user-approved): Beer→**Lemonade** (לימונדה 🍋), Wine→**Milkshake** (מילקשייק 🥤), Champagne→**Hot Chocolate** (שוקו חם ☕). Bacon (only pork item) and the `No smoking` sign reviewed and **kept** (user call). Added nikud for the new words via `build-nikud-map.py`. Also hardened the build script to skip `tests/` + `*.test.*`/`*.spec.*` files so test-fixture Hebrew never bleeds into the production map (removed one stray `כותרת`). Game-context weapons (gaming/minecraft), medical tools, and `blood` were judged contextually fine for the audience.

## Theme D — Layout / visual polish

- [x] 🟡 **D1** Word Journey — stage path should sit inside the progress strip. **FIXED 2026-06-08:** added an optional `center` slot to `QuestionProgress` (rendered between the "שאלה N מתוך M" label on the right and the `אפס משחק` reset button on the left); moved `WJStageBar` out of the GameHero `heroAside` into that center slot. Title is now clean; the stage bar is centered in the progress strip. Screenshot-verified; `wj-step1` + lifecycle suites pass.
- [x] 🟡 **D2** Home — top hero should be a **single row**. **FIXED 2026-06-08:** the inner stat-chips + "בוא נשחק" CTA group had `flex-wrap`, so the CTA dropped to a second line. Added `sm:flex-nowrap` so on sm+ the three chips and the CTA stay on one row beside the greeting+mascot. Screenshot-verified; mobile still wraps.
- [x] 🟡 **D3** Reading — the English word showed then vanished, sliding the letter-picker up. **FIXED 2026-06-08:** added `wordHidden` to `MediaPromptCard` (renders the word with `visibility:hidden` so its layout box is reserved); Reading passes `wordHidden={!wordVisible}` instead of dropping the word, so nothing shifts.
- [x] 🟡 **D4** Scramble — the "right order" summary on a wrong answer overwrote the attempt in place. **FIXED 2026-06-08:** new shared `SentenceComparison` (word-chip sibling of `SpellingComparison`) shows the correct order (green) above the child's attempt (green per right-position word, red otherwise). Replaced the staggered in-zone reveal. Screenshot-verified + test asserts the panel.
- [x] 🟡 **D5** Grammar-beginner — restructured the speaker/select option affordances. **FIXED 2026-06-08:** in `CompleteSoundView` + `SoundsRightView` the speaker is now a play-only icon on the LEFT (`dir="ltr"` group) and the option label itself is the wide select button with a blue ✓. Screenshot-verified.

## Theme E — Game flow / mechanics (some need decisions)

- [x] 🔴 **E1** Word Journey finish — `תרגול מילים שנלמדו` button: unclear purpose. **FIXED 2026-06-08 (removed):** confirmed the button was mislabeled + broken — `start({ replay: true })` only halved coins, the pool still came from `getWordJourneyWords()` (fresh New/Learning words), never the learned ones. Removed it (+ the `hasLearnedWords`/`onPractice` props + dead `learnedCount`). The finish screen keeps "מסע נוסף" + "חזרה הביתה"; the dedicated Practice tile on Home already serves review. (Replay-mode bridge plumbing left in place, now always-off — harmless.)
- [x] 🟡 **E2** Practice — re-entry doesn't save question-count progress. **WORKING AS INTENDED 2026-06-08:** no-resume is deliberate (legacy parity + Practice is the Due-first review surface, each run a fresh pull; resuming a stale position is less useful). Documented in `bridge/practice.ts`. No change.
- [x] 🔴 **E3** Story Time — post-story question: the correct option sat in a **fixed slot every time**. **FIXED 2026-06-07:** root cause was that the data authors *every* question's `correctIndex: 0` and nothing downstream shuffled (perceived as "third always correct"). Added `shuffleQuestionOptions()` in `src/bridge/story-time.ts` — Fisher-Yates per question with `correctIndex` remapped to the correct value's new position (options are de-duped upstream, so `indexOf` is unambiguous). Pure (no source mutation). Tests: `src/bridge/story-time.shuffle.test.ts` (4 unit) + existing happy-path Playwright still passes (it clicks `options[correctIndex]`).
- [x] 🟡 **E4** Story Time — make clickable words more prominent + play both audios. **FIXED 2026-06-08:** clickable words now render as a rounded pill with a 🔊 speaker glyph + blue ring (matches the app's pill language, signals audio). On tap: Hebrew tooltip + play **English then Hebrew** (`speakWord` → `speakHebrew`); tooltip duration extended to cover both. Screenshot-verified.
- [x] 🟡 **E5** Story Time — case (upper/lower) toggle has no effect on the sentences in question. **FIXED 2026-06-07:** `StoryReadPhase` now reads `caseMode` from `useTextPrefs` and applies a **display-only** transform to each rendered token (both highlighted-word buttons and plain spans). Audio paths (`speak(sentence)`, `speakWord(h.word.toLowerCase())`) keep the **original** strings — uppercase text fed to TTS gets spelled out letter-by-letter (`feedback_tts_uppercase_spells_letters`). Distinct from the nikud work.
- [x] 🟢 **E6** Story Time — engine should detect **new/unknown words** in a story and auto-highlight them (underline + translation + audio per E4). **SHIPPED 2026-06-08:** `buildStoryFromTemplate` (`data/stories.js`) now scans the rendered sentences against the vocabulary bank and registers every word **not in the child's learned set** as a highlight (exact match + a shallow `-s` stem so `runs`/`cats`→`run`/`cat`). Those render as the existing tappable blue pills (translation + English→Hebrew audio) and populate the "מילים חדשות בסיפור" `WordTable` — no component change needed (the read-phase renderer already matched any token against `story.highlights`). **Product decisions (user, 2026-06-08):** (1) *only NEW words* highlight — learned slot words render plain; (2) *exposure-only* — new words are NOT recorded toward mastery. Recording was split out: new `story.reinforceWords` (the learned slot words) drives `recordWordAttempt` on a correct quiz answer; `story.highlights` is display-only. Resolves C6. Data-layer proof + Playwright/screenshot verified.
- [x] 🟢 **E7** Grammar — full correct Hebrew sentence + word/translation/play table. **SHIPPED 2026-06-08:** (1) table half via `WordTable` after every answer (see F3); (2) the Hebrew sentence now always renders in FULL — `hebrewSentenceDisplay` strips the `___` (+ tidies whitespace/punctuation) so the child reads the complete meaning instead of deducing from context (Hebrew copula sentences omit "to be", so removing the blank yields the natural sentence; question forms read fine too). The English blank stays the exercise; the Hebrew doesn't reveal the English answer. Test asserts no `___` in `grammar-hebrew`.
- [x] 🟢 **E8** Grammar — redesign as a "new words" learning surface (underline + translation + audio table). **SHIPPED 2026-06-09:** new shared `src/bridge/newWords.ts` `detectNewWords()` (the E6 rule, generalized: bank words in the sentence NOT in the learned set, exact + shallow `-s` stem, translation from the bank, exposure-only) + shared `NewWordPill`/`SentenceText` (`src/features/games/shared/NewWordPill.tsx`). Wired into **Grammar** (`GrammarGamePage`) **and** **Articles + Progressive** (`BlankFillGamePage`). **Product decisions (user, 2026-06-08):** D1 blue pills (Story-Time look), D2 always-interactive (incl. while choosing), D3 one merged after-answer `WordTable` (correct ✓ + chosen ✗ + new words, no mark), D4 all three blank-fill games. Spec `docs/grammar-new-words-spec.md`. Tests: new "E8: new vocabulary words surface as tappable pills" + updated F3 table-count assertion; screenshot-verified.

## Theme F — New features

- [x] 🟢 **F1** Reading — **"sneak peek"**: a ~½-second reveal of the word after it vanishes, **configurable in settings**. **SHIPPED 2026-06-08:** a child-tapped `👁 הצצה (N)` button in the Reading awaiting-actions row re-flashes the auto-hidden English word, then re-hides it. Disabled while the word is still auto-visible or the per-question budget is spent. Three settings (Game tab → "הצצה חטופה (קריאה)"): `sneakPeekEnabled` (on by default), `sneakPeekDuration` (0.3–2s slider, default 0.5), `sneakPeekBudget` (1–5 peeks/question, default 3) — all in `AppSettings`/`DEFAULT_SETTINGS`. Budget resets per question (not persisted across refresh, unlike the audio budget). Config read once per session via `getSettings()`.
- [x] 🟢 **F2** Fill-blanks — after answer selection, show a **word summary table** (answer word + Hebrew translation + play button) below the answers. **SHIPPED 2026-06-09:** unblocked the per-option-Hebrew gap with a shared `optionTranslations` map (lowercased word → Hebrew, all 184 unique fill-blanks option words) + `getOptionHebrew(word)` in `data/sentences.js`, re-exported via `bridge/fill-blanks.ts`. `FillBlanksGamePage` renders the shared `WordTable` after answering — correct option (✓) + chosen-wrong option (✗) with Hebrew gloss + play, mirroring Grammar (F3/§8). Glosses authored without nikud; re-ran `build-nikud-map.py` (3366→3409) + **8 manual homograph overrides** where Dicta returned the noun/imperative voweling instead of the verb (`צד` hunts, `רב` quarrels, `שר` sings, `נחת` landed, `שכח` forgot, `משחק` plays, `שוחה` swims, `מוצא` finds — survive re-runs per C5). Tests: 2 new in `react-routes.spec.js` (table+correct row; 2-row wrong pick); screenshot `bug-screenshots/F2-fillblanks-word-table.png`.
- [x] 🟢 **F3** Story Time / Grammar — shared **word + translation + play-button table**. **SHIPPED 2026-06-08:** new `src/features/games/shared/WordTable.tsx` (per `docs/word-table-spec.md`) — Hebrew (right) · English+✓/✗ (middle) · 🔊 play (left); owns case toggle, nikud, de-dupe, English→Hebrew audio. Wired into **Story Time** read phase (highlights list, title "המילים בסיפור") and **Grammar** after every answer (correct ✓ + the chosen ✗ when wrong). Screenshot-verified; assertions added to both suites.

## Theme G — Performance (known)

- [ ] 🔴 **G1** Confetti animation slow + mouse-movement freeze (CPU overload) on Practice and any recording game. **Known UNSOLVED** — see `project_confetti_first_burst_lag` memory; 3 fixes tried & reverted. Profile before re-attempting. Screen recording available.

---

### Quick stats
31 items (29 dumped + A11, C7 from screenshot review) · Nikud cluster (A) is the biggest and likely shares fixes · remaining `[~]` items still needing a product decision / assets: C2/C3 (real pictures), A11 (confirm intent). [E6 + C6 shipped 2026-06-08; E8 + F2 shipped 2026-06-09.]

---

## Screenshot mapping (2026-06-07)

All files in `bug-screenshots/`. `+` = one shot covers multiple IDs.

| Screenshot file | Bug ID(s) | What it confirms |
|---|---|---|
| `C4-abc-question-mark.png` | C4 | ABC "listen & choose the letter" subtype renders the prompt as a literal **`?`** with options Mm/Zz/Aa/Dd. (May be *intended* audio-only — see new finding N1.) |
| `D2-home-hero.png` | D2 | Home top: owl greeting + 3 stat pills + green continue button stack over several rows. |
| `A1-listening-milliard.png` | A1 | Listening: `1000000000` + `מִילְיַארְד` shown **with** nikud; answer = BILLION. |
| `C2-wj-cannon-target-a-picture.png` | C2 | Word Journey picture-choose: correct (✓) image is a 🎯 dartboard. |
| `C2-wj-cannon-target-b-spell.png` | C2 | Same word, spell stage: word is `תּוֹתָח` (cannon) but image is 🎯 — **cannon ↔ target mismatch**. |
| `A2+E1-wj-finish-screen.png` | A2, E1 | WJ "מַסָּע הֻשְׁלַם" finish: `נלמד` tags + the `תרגול מילים שלמדו` button. |
| `C1-listening-dozen-asara.png` | C1 | Listening: answer DOZEN, image shows **12** (tiles 1‑2), but Hebrew = `עֲשָׂרָה` (ten). Should be תריסר/12. |
| `A3-memory-level1-complete.png` | A3 | Memory level-1 complete state (board + "כל הזוגות נמצאו" toast). |
| `D5+B2-grammar-beginner-options.png` | D5, B2 | Grammar-beginner "מה נשמע נכון?": rows show ✓ (left) + 🔊 (right); answer labels render as **`אפשרות 1/2/3`** placeholders (see N2). |
| `D5-grammar-beginner-she-is-happy.png` | D5 | Grammar-beginner blank-fill "SHE IS HAPPY / היא שמחה"; ARE/IS/AM each with ✓+🔊. |
| `A4-articles-she-eats-apple.png` | A4 | Articles: `היא אוכלת תפוח` + option sub-labels (`דבר מסוים`…) all **no nikud**. |
| `A5-progressive-was-sleeping.png` | A5, A6 | Progressive: `אתמול בלילה היא ישנה` + note `"Last night"=עבר…` + option sub-labels — no nikud. |
| `A6-progressive-was-riding.png` | A5, A6 | Progressive: `אתמול הוא רכב על אופניים` + note + sub-labels — no nikud. |
| `D3-reading-dinosaur.png` | D3 | Reading: `DINOSAUR`/`דִּינוֹזָאוּר` shown above empty letter slots (the word that later vanishes → layout shift). |
| `E4-storytime-underline-beans-aunt.png` | E4 | Story Time "הבוקר שלי": `Beans` / `Aunt` underlined; hint "click highlighted words". |
| `E4-storytime-translation-popup.png` | E4 | Same, after click: tiny `שעועית` tooltip under `Beans.` — too subtle. |
| `E3-storytime-third-always-correct.png` | E3 | Post-story Q "לאן אני הולך?": park/home/**school**; school (right-most) selected/correct. |
| `E6-storytime-koala-new-words.png` | E6 | Koala story: `park` + `fast` NOT underlined (engine doesn't know them); `Koala`/`Salad` are. |
| `A7+F2-fillblanks-ani-ben-shesh.png` | A7, F2 | Fill-blanks: `אני בן שש` no nikud; answers seven/eight/six (candidate words for the F2 table). |
| `D4-scramble-wrong-answer-summary.png` | D4 | Scramble wrong answer for `השמיים כחולים`: faint/unclear "right order" summary. |
| `E7-grammar-hebrew-blank.png` | E7 | Grammar: Hebrew `___ הלכת לישון?` carries a blank (user wants full Hebrew sentence). |
| `E8+B5+A8-grammar-brackets.png` | E8, B5, A8 | Grammar "our house": brackets `(we)` / `(לפני שם עצם)` (read aloud by TTS); Hebrew option sub-labels no nikud. |
| `A11-wj-champagne-toggle-a.png` | A11, C7 | WJ recognize-word stage, `CHAMPAGNE`/`שַׁמְפַּנְיָה`. Toggle-no-effect (assumed) + content concern. |
| `A11-wj-champagne-toggle-b.png` | A11, C7 | Near-duplicate, case toggle differs — shown word unchanged. |
| `G1-confetti-cpu-recording.mov` | G1 | Screen recording of confetti lag + mouse-freeze on a recording game. |

### No-screenshot items (from the original dump)
B1 (picture-match autoplay), B3/B4 (pronunciation autoplay/auto-advance), C3 (creeper image), C5 (goat nikud), E2 (practice progress save), A9/A10 (story nikud / `השמעות שנותרו` — visible incidentally in many shots), D1 (WJ progress path), E5 (story case toggle). These stand as described; no extra visual needed.

### New findings surfaced by the screenshots — all RESOLVED into tracked items
- **N1 → C4 (resolved).** Keep the letter hidden (audio-only is intentional) but replace `?` with a 🔊 glyph.
- **N2 → D5 (resolved).** grammar-beginner answers render as literal `אפשרות 1/2/3` — folded into the D5 redesign (select-the-word + speaker-left).
- **N3 → C7 (resolved).** Content audit for age-inappropriate vocab (`CHAMPAGNE`); sweep the bank for similar.
- **Q1 → A11 (resolved, ASSUMED).** The two champagne shots logged as a WJ recognize-stage toggle-no-effect bug — confirm intent when convenient.
